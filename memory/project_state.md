# Project state

## Threads Shopping MVP — 2026-09-08

- Continues isolated `shopping-social-s2a` above local S2-A checkpoint `450ea56` (not deployed separately). User authorizes final commit, fast-forward main push and existing Vercel Git production deployment after validation. Other worktrees and canonical copies are intentionally untouched for this release.
- Shopping now has 貼連結辨識 / 截圖辨識 / 手動新增. Threads links use `social-source` with `recognize: true`: the reader supplies verified primary-image bytes and bound caption to the existing single Shopping Vision/structured-output request. Multiple primary images are evidence for one product, never split into entries. Default `social-source` remains read-only without an OpenAI key. S1 product URLs use `url-draft` and the same confirmation editor, with an optional small verified image preview.
- Blocked, unsupported or insufficient sources retain the original URL in the same pending entry and offer 上傳截圖辨識. Screenshot fallback reuses the original screenshot recognition request, image compression and confirmation/save flow. Uncertain recognition requires manual input; linked items can retain unknown currency. No outbound product-link following, Instagram enhancement, migration or second AI flow.
- Optional `sourceReferenceUrl` and `sourceType` (`threads`, `product_url`, `screenshot`) survive private Shopping PUT/GET, reload and item edits. Legacy items remain valid. Async import and save work checks entry/trip/member/context before applying results; submit locks while image preparation completes.
- Safe source HTML remains identity-only, with the existing independent 4 MiB byte cap; compressed responses remain rejected. Earlier real Threads checks of GhWXu9wLy and E7ZmNknYI each yielded partial metadata with caption plus one verified primary image. No decompression change or anti-bot bypass.
- Local headless Edge at 393×852 exercised Threads → edit → save → reload, blocked → screenshot on the same entry → save, and S1 → editor → save with mocked APIs. Three saved items retained their sources, no page errors/overflow; URL input 16px, read action 50px. Production App is reserved for the user's manual acceptance.
- Validation: targeted 178/178; one full regression run 322/323, with the sole failure caused by the old startup test's fake form omitting native `dataset`. Only that fixture was corrected (also supplying `isConnected`); startup targeted rerun 22/22 including the failing case. No product code changed after the full run and no second full run. Syntax/diff checks pass; API functions 12. Fetch confirmed origin/main `8632d0491cff9c4d235d3c3d1951de820db4f5a9` and fast-forward ancestry. Protected worktree heads and tracked diffs are unchanged.

## Shopping Social Import — S2-A (2026-09-08, local implementation only)

- Fetched `origin/main` and verified deployed S1 baseline `8632d0491cff9c4d235d3c3d1951de820db4f5a9`; created clean `shopping-social-s2a` branch/worktree. This checkpoint stays isolated per explicit instructions; no canonical-source/mirror sync, stage, commit, push, deployment or production App access.
- Existing `POST /api/shopping-recognize` adds `{ action: "social-source", tripId, sourceUrl }` returning `{ socialDraft }` after existing member/trip authorization. No OpenAI key, Vision, similar-image search, S1 product-page parsing of outbound links or persistence. API functions remain 12.
- New `lib/threads-source.mjs` reuses S1 `safeFetchProductResource` without editing it: all DNS/IP validation, pinned socket IP, verified TLS and per-redirect checks remain. HTML has an independent **4 MiB / 15-second / 4-redirect** policy, with declared and streamed byte limits; compressed responses remain unsupported. Too-large HTML returns **partial / too_large**, keeps original URL and the last reached response URL, and never parses truncated HTML. Threads host/path checks additionally prevent cross-platform or different-post redirects.
- Draft contract includes originalReferenceUrl, finalUrl, sourceCanonicalUrl, sourceHost, postId, caption, metadata, imageCandidates, outboundLinkEvidence, fieldEvidence, readStatus, readReason and fetchedAt. Original URL is never replaced by final/canonical. Canonical and head OG must agree with the final post identity. Structured caption/media require an exact SocialMediaPosting URL or Threads JSON post code plus author username; unrelated DOM images, recommendations, avatar paths, inert markup and arbitrary script text are excluded. Missing/ambiguous metadata is not guessed.
- Images are evidence candidates, not product identities: at most **6**, deduped by CDN host/path, **2 workers**, each **2 MiB / 4 seconds / 3 redirects** with S1 SSRF checks plus Meta post-photo CDN/path checks and MIME/signature validation. Image bytes are discarded after validation. Candidate readStatus distinguishes successful reads from failures; future consumers must only use `readStatus: success`. Image failure makes the draft partial without losing its caption. At most 12 MiB accepted media per request.
- Outbound links are extracted only from the bound caption and returned as unfetched evidence; navigation/recommendation links are excluded. DNS/content/product identity of outbound links is intentionally unverified, and no outbound product fetch occurs in S2-A.
- Status: success = usable source metadata without detected gaps; partial = incomplete/ambiguous metadata, image failure, limit or potentially truncated caption; blocked = refusal/challenge or blocked fetch with specific reason; unsupported = unsupported URL/type/encoding or normal page without usable metadata. These are source-reader outcomes, never claims that a product was identified.
- Offline fixtures: GhWXu9wLy preserves the previously observed redirect to `@mio_1983/post/DRNBp0REvzg`; caption/images are explicitly synthetic because the earlier 1 MiB read stopped before metadata extraction. Its test pads HTML beyond 1 MiB. E7ZmNknYI preserves observed caption and canonical `@yutong_0422/post/DRM43y2gch0`, with deterministic replacement CDN URL. Neither is a full production snapshot; tests never depend on live Threads. The earlier manual read established the second image was HTTP 200/JPEG/123179 bytes; no new live smoke or product recognition is claimed here.
- Validation from `shopping-social-s2a`: **34/34 S2-A tests**, combined S2-A/S1/screenshot targeted **94/94**; exactly one full `node --test` **308/308**; syntax and diff checks pass; functions **12**. No UI, pending imports, screenshot fallback UI, Shopping schema, Instagram enhancement, duplicate handling, places or lodging changes.

## Shopping URL Import — Checkpoint S1 (2026-09-07, commit review only)

- Fetched and verified `origin/main = 1eafd8a266c0690b76c3f1f79c258cb606f83f67`; created a clean `shopping-url-s1` branch/worktree from it. Explicit S1 instructions suspend the usual canonical-source/mirror publication workflow for this checkpoint. Ticket A is closed; no Ticket A/B work was reopened.
- `POST /api/shopping-recognize` accepts `{ action: "url-draft", tripId, sourceUrl }` and returns `{ draft }` after existing session/member/trip authorization. It needs no OpenAI key, invokes no OpenAI or similar-image search, and does not persist Shopping items. Source outcomes use `draft.readStatus`/`readReason`; authorization retains HTTP 401/400/403. API functions remain 12.
- `lib/product-page.mjs` exports a pure parser and a bounded reader: HTTPS port 443 only, no URL credentials, public IPv4/IPv6 validation, all DNS answers checked, socket lookup pinned to the validated IP, verified TLS and no pooled connections. Every redirect is revalidated (maximum 4). DNS/TLS/body/redirects share a 12-second HTML deadline and 1 MiB limit; selected images use the same guard with 5 seconds and 2 MiB. MIME and image signatures are checked; compressed responses are rejected. No caller cookies or credentials are forwarded.
- Identity: page mainEntity/mainEntityOfPage, then matching Product URL, then a sole unambiguous Product. Supports JSON-LD arrays, @graph references and explicitly selected ProductGroup variants. Recommendation-only ItemList products are ineligible. Name/brand/image/offer/IDs never borrow from another identity. Ambiguous JSON-LD is not repaired with page-wide OG; product-typed OG is a conservative partial fallback.
- Original URL survives redirects exactly. Canonical requires explicit page declaration, same host and selected-product evidence. Only utm_* / gclid / fbclid are normalized for identity comparison; variant queries remain significant. SKU/productID retain type and host/product scope; arbitrary query parameters never become IDs.
- Price remains null for multiple offers, ranges, membership restrictions, unknown currency, unresolved/mismatched itemOffered, conflicting/conditional specifications, inactive offers or unresolved variants. OG price is evidence only. Unknown fields remain null. Trusted name/image/price can yield `success`; missing/ambiguous fields yield `partial`; actual challenges/refusals/blocked fetches yield `blocked`; ordinary HTML without reliable product metadata yields `unsupported`. Image failure retains text/price and records image-specific failure evidence.
- Validation from this isolated worktree: targeted `node --test tests/product-page.test.mjs tests/shopping-recognize.test.mjs` **60/60** (54 S1 + 6 legacy screenshot recognition); exactly one full `node --test` regression **274/274**, including all existing screenshot Shopping tests; source/test syntax checks and `git diff --check` pass; API functions **12**.
- Limits: deterministic fixtures and mocked DNS/HTTPS/Redis handler tests, no live retailer acceptance or production App opened. Client-rendered pages, compressed-only responses and absent/ambiguous metadata intentionally do not trigger bypasses. No UI, persistence, duplicate UI, touched editor or screenshot fallback UI added. No staging, commit, push or deployment. All changes stay isolated pending explicit S1 commit review.
- Fetch design references: [OWASP SSRF prevention](https://cheatsheetseries.owasp.org/cheatsheets/Server_Side_Request_Forgery_Prevention_Cheat_Sheet.html), [Node HTTPS options](https://nodejs.org/api/https.html).

## P1 Maps/text address import — 2026-09-06

- Production acceptance of `0fde78c` failed on the exact Katase/Fujisawa plus Room 202/Okubo paste. The first failing decision was `streetHouse: false` for `3-chōme-8-12`; the Room/Okubo line already correctly had both streetHouse and roomFloor set. The unrecognized first address was prepended to the sole Okubo core, so one contaminated candidate reached enrichment. The follow-up hotfix recognizes the hyphenated romanized Japanese chōme core form without changing grouping, API, selection, or submission logic. The preserved blank paragraph then separates two recognized cores rather than sitting inside one merged candidate.
- Follow-up validation covers the exact production input and both cancellation directions: targeted 15/15, necessary regression 136/136, app syntax and diff check pass; API functions remain 12. The earlier 134/134 result below describes the original P1 release candidate and did not cover this production failure. Production UI acceptance remains manual.

- The P1 release candidate starts at `7dcaa8c`, retaining the released P0 identity protection and Google Maps button behavior. Its scope is limited to Maps/text address parsing, candidate selection, and corresponding regression coverage.
- Pure-text import now groups around each independently recognized street/house-number core while retaining original line and blank-paragraph structure. Administrative/postal continuation lines close the preceding core; leading room/floor evidence belongs to the following sole core. A second core always starts a second candidate even when its city or postal code matches. Component-only room/floor/postal/admin text yields no place, and ambiguous room/floor or unknown text between two cores remains visible as an unresolved, unselected, non-geocoded item. Maps links remain URL-driven, so attached address lines do not create another candidate.
- Ordinary Maps/text/shared-list rows have explicit selected state and independent checkboxes, including selection in the preview. All import eligibility requires `selected === true`; the existing deduplicated `submittablePlaceImports` list drives counts and actual submission. Social branch multiselect and lodging alternative single-select remain unchanged.
- Thirteen targeted P1 tests plus necessary regression pass 134/134. This includes A–F grouping, ambiguous-component geocoding exclusion, independent cancellation and actual submit count. App/test/API syntax and `git diff --check` pass; API functions remain 12. No browser or production UI acceptance is claimed; production UI acceptance remains manual.
- Parsing remains heuristic. International address cores using street words outside the supported set, house-name-only addresses, UK-style alphanumeric postcodes, and formats that split an unmarked house number from an unfamiliar street name may remain separate text candidates instead of being grouped. P2 lodging draft and P3 rematch remain outside this round.

## Implemented

- Public responsive web app optimized for iPhone 15 Pro.
- Overview is a decision dashboard with complete trip title/date text, trip status, a planning-completion percentage derived from flights, lodging, itinerary-day coverage, and transport coverage, prioritized next actions, and a full editable flight list with an add-flight entry point. Days until departure remain a separate status fact and do not affect completion.
- Four-digit PIN identities persist across devices; guest mode is read-only.
- Multiple private trips per member, invite-code joining, trip switching, member removal, leaving a trip, and native share links.
- Shared Redis-backed trip data with revision-aware updates.
- Places are separated into attractions, restaurants, lodging, and shopping. Shopping places cover stores such as apparel, footwear, department stores, malls, boutiques, and drugstores; they remain shared map/itinerary places and are distinct from each member's private Shopping checklist. Travel Area lists group exclusively by `travelAreaKey` and render `travelAreaZh（travelAreaLocal）`; raw address and administrative fields remain separate evidence.
- Travel Area is a traveler-facing same-day planning concept, not a Google administrative component and not a forced ward/district/municipality level. Japan mixes recognizable neighborhoods, wards, and cities as needed. Required mappings are 神宮前/神南 → `澀谷（渋谷）`, 花川戶/雷門 → `淺草（浅草）`, 銀座 → `銀座（銀座）`, 練馬區 → `練馬（練馬）`, 鎌倉市 → `鎌倉（鎌倉）`; Korea and the US retain the narrower existing travel granularity, including 明洞 and Brooklyn.
- Automatic Travel Area resolution version 5 invalidates older automatic results, including version 2 and 3 completion markers, without overwriting manual assignments. Recognition succeeds only when the final product-level key and both display names match a Travel Area rule. Failed refreshes retain the previous usable group and expose retry/manual-edit state instead of permanently displaying `正在辨識地區`. Records containing only local-language raw components are also migration candidates.
- Private lodging and any other custom place can be created without accepting a Google business candidate. The editor stores the user's own name, complete address, optional Maps link, category/type, Chinese/local area labels, and one compressed shared photo. The address is geocoded directly rather than searched as a nearby business. Every saved place can later be edited from its detail sheet, and renaming cascades into votes, itinerary items, transport labels, and the active map selection.
- Google Maps single-place links, multiple links, and public shared lists can be imported and enriched with name, area, category, hours, phone, photos, and coordinates. Coordinate-only links in decimal or degree-minute-second form are never displayed as encoded place names: they are reverse-geocoded through Google when available, with a user-triggered OpenStreetMap fallback when Google address geocoding is unavailable, into a readable `地址位置｜…` label. They remain explicitly identified as an address rather than a lodging name and open a pre-import preview with an embedded map, full address, and required OpenStreetMap attribution when applicable.
- Instagram posts, Reels, Threads links, and standalone screenshots can be imported through an authenticated AI-assisted review flow. Public post text plus up to twenty safely allowlisted main-post images is sent through one visual-recognition request; Threads carousel images embedded after a video cover are included while profile avatars are excluded. An optional user screenshot remains the fallback when the public page withholds the relevant media. When a creator hides a lodging name in a profile link but states an address, the address and lodging clues remain searchable and may receive a conditional web cross-check. Before saving, tapping anywhere on a Google Places candidate card except its selection radio opens a nested preview with photos, full address, rating, hours, phone, and a Google Maps link; the user then selects a match and confirms the final import, while the original post link is retained on the place.
- Agoda, Booking.com, and Airbnb property links use the same confirmed import flow. Supported links are safely allowlisted, automatically treated as lodging, web-checked when public page metadata is incomplete, matched to Google Maps candidates, and retained as the original booking reference after the user confirms a match. Booking structured page data is inspected for a full postal address and passed directly into recognition even when Open Graph text or AI output omits it. Descriptor-heavy Booking titles may expose a short trailing official property name, which is retained instead of the full room-description title. If an apartment has no independent Google business page, its verified full address can instead produce a clearly labeled lodging-coordinate candidate for navigation, with a bounded OpenStreetMap address search when Google returns no usable coordinate. Explicit lodging name, address, Google Maps link, and Booking link pasted together are merged into one record. Null and `0,0` coordinates are rejected. Japanese postal codes and complete house numbers from the source page take priority over AI/web-search guesses; mismatched nearby properties are removed and the exact coordinate candidate is ranked first. The user must still verify the address and confirm it. When Booking answers the server fetch with an anti-bot challenge page (no metadata at all), a host message pasted with the link still works: a labelled lodging name (公寓名稱／飯店名稱：…) becomes the candidate name, the labelled address drives the exact coordinate lookup, house-number matching accepts Google's romanized Japanese addresses (1-chōme-16-19 Ōkubo), and the Booking URL slug is passed to AI web search as a transliterated title clue.
- One social post can return and import up to twenty distinct place mentions. Google candidate lookups run with a five-request concurrency ceiling, preserve result order, and keep partial successful matches when another lookup fails.
- Social import review groups Google Maps alternatives under the AI-recognized place they belong to. It separately reports recognized-place count and candidate-row count, preselects one valid match per place, and labels the final action `加入全部 N 個地點` when every recognized group has a valid selection. Each group can instead run an independent editable Google Maps rematch that excludes previously shown Place IDs, or be explicitly skipped without forcing a wrong candidate; other recognized groups remain untouched and no second whole-post AI call is made.
- Every social candidate group now exposes a source-comparison sheet before import. The same recognition request returns the original caption, an AI evidence summary, and up to four attachment indexes that directly support that place; related original images can be enlarged, and the full original post remains available as a fallback. These transient source images are removed before the selected place is saved.
- Google Maps actions adapt to the device: phones/tablets navigate in the current context so the native Maps app takes over. Desktop browsers first open a same-origin blank tab, sever its opener, and replace that tab's location with Google Maps, keeping the App page open. Only a genuinely blocked popup or failed new-tab navigation falls back to the App tab; the App never treats the `null` return value caused by a `noopener` feature as proof that the popup was blocked. Place details, airport map nodes, transport route buttons, and route markers all use this one guarded handoff.
- The four bottom-navigation buttons use meaningful Tabler travel icons (compass, map pin, calendar event, shopping bag) instead of abstract geometry. Text labels remain visible, the selected button uses the existing brand accent, `aria-current="page"` follows both direct tab changes and internal navigation, touch targets remain at least 54px high, and reduced-motion/focus states are explicit.
- The App ships its own PWA icon set (192/512 any, 512 maskable, 180 apple-touch-icon) and its own copy of Leaflet 1.9.4 under `vendor/leaflet/`; no map or style asset loads from a third-party CDN.
- The manifest now declares a standards-based GET Web Share Target at `/share-target`. On supporting installed-PWA platforms, shared Agoda, Booking.com, Airbnb, Instagram, Threads, and Google Maps links are safely extracted, held through login/trip loading when necessary, and automatically sent into the same candidate-review and explicit-confirmation flow used by pasted links. The visible pasted-link and screenshot fallbacks remain unchanged. WebKit still does not implement Web Share Target, so this receiver cannot appear in the iPhone system share sheet until Apple adds support; direct iPhone delivery still requires an iOS Shortcut or native Share Extension.
- Frontend HTML and static assets explicitly revalidate on Vercel. Versioned asset URLs remove stale browser cache entries, and long-lived tabs check the current app asset when returning to the foreground and reload after a deployment change.
- Place details include photos, Google Maps link, votes, itinerary assignment, business hours, phone, editable notes, and an editor-only confirmed delete action that works whether the detail was opened from the list or map. Japanese restaurant imports persist a validated Tabelog URL at confirmation time. The five seeded Tokyo restaurants resolve to exact Tabelog restaurant pages; other new Japanese restaurants retain a safe search URL. Details place a separate orange-and-white `Tabelog預約` action immediately below the phone card in the same grid column, with exactly the same column width and a fixed 44px height. The action is an ordinary HTTPS Universal Link on Tabelog multilingual App's official `tabelog-tourists.onelink.me/3eEh` domain. It carries the exact `tabelog-tourists://rstdtl/{id}/top/` App destination inside Tabelog's published OneLink parameters and uses the equivalent Traditional Chinese page for iOS, Android, and web fallback; the travel App never navigates directly to the custom scheme.
- Place-detail source buttons are derived from a safely recognized Instagram, Threads, Agoda, Booking.com, or Airbnb reference host rather than trusting a stored platform label; Google Maps URLs can never appear as social references. Google Maps actions navigate in the current browser context so iPhone app handoff does not leave an empty Safari tab.
- Planning map and day map are independent. The map can enter an App-level fullscreen workspace with a collapsible left filter/place rail on desktop and an overlay drawer on narrow screens. Compact markers and rail results pan to a place and open an animated bottom preview card with photo, rating/recommendation, type, and area before the existing full detail sheet. The planning map has an optional device-only live-location layer with a centered control icon and accuracy radius; day maps retain their route-specific behavior.
- Daily itinerary supports time-wheel confirmation, chronological sorting, touch drag reordering, swipe deletion, add-place selection, and transport segments between adjacent items.
- Transport segments adapt fields to walking, subway, train, bus, taxi, driving, ferry, and other modes. Walking pre-fills a coordinate-based time estimate; fixed-schedule, fare, traveler, and booking fields stay collapsed until “指定票券” is enabled.
- One-level undo restores the latest reversible trip edit and is available from success toasts, every main page, and every editable sheet.
- Flights support outbound, return, round-trip, and ordered transfer records. Each direction can contain up to six persisted legs grouped by journey ID; legacy single-leg records remain compatible, layover duration is shown on overview cards, and multi-leg itinerary entries follow each leg's actual departure date.
- Flight airports are selected from city-aware options; common Taiwan, Japan, Korea, and regional airports are included.
- Overview flight cards use a compact layout with the outbound/return marker fixed at the far left and a larger vertically centered passenger note.
- Flight forms allocate more horizontal space to airport selectors than city fields so full airport names remain readable on iPhone.
- Flight date/time values use centered app-rendered labels over native iPhone pickers, avoiding Safari's top-aligned text.
- Flight ticket images can be selected or photographed, recognized locally in the browser, and used to prefill the flight form before confirmation.
- A fourth Shopping tab stores a separate private list for each signed-in member and trip. Shopping data never enters the shared trip payload.
- Shopping items support brand, product name, benefits/recommendation notes, editable reference price and ISO currency, removable default/custom categories, reusable removable recipient tags, notes, purchased/unpurchased state, higher-resolution reference photos, detail/edit/delete flows, one-level private undo, confirmed left-swipe deletion, and batch selection/deletion. Manual add/edit forms can select or photograph a private product reference image, preview it before saving, replace it, and remove it later. Estimated totals are grouped by currency without automatic conversion.
- New screenshot imports use Responses for multilingual identification and web-grounded product verification, then extract product photos from matching official or retail pages. Image generation has been removed. Each recognized product shows up to three web candidates, defaults to the first, and offers `換一批圖片` to retrieve a different round while excluding already-seen candidate IDs. Candidate thumbnails open a centered large preview with previous/next controls and selection. If the primary product-page search returns fewer than three usable images, two distinct backup searches now run concurrently and their sources are interleaved before extraction.
- Import review shows the three-choice product-image picker first, editable brand/product/benefits/category fields and concise AI annotation next, and the member's original screenshot last in a collapsed disclosure. Only the selected candidate is compressed and saved; detail views keep readable text, strip URLs/domains/source citations, and show no stars or recommendation index.
- Up to twelve recommendation screenshots can be accepted at once and their AI recognition requests are dispatched concurrently rather than sequentially. iOS may still let a user tick more than twelve inside its native picker because the web file input cannot set a native selection cap; after returning to the App, an over-limit selection is rejected with an explicit count. Every product card has its own queued, image-preparation, AI-recognition, complete, or failed progress state. Each completed card independently unlocks image refresh, and the chosen web image is exported as iPhone-safe JPEG, verified against the server's sanitized save response, and displayed in both the list and detail sheet.

## Latest validation

- 2026-09-04 production closeout: Travel Area v5 is successfully deployed and complete. The user-confirmed `東京 7 日` production smoke test loaded all 31 places and completed the normal v5 migration with 14 bilingual `繁體中文（當地語言）` groups. Shibuya contains the expected five places and Asakusa the expected three; no 神宮前、神南、花川戶／花川戸、雷門 or permanent `正在辨識地區` group remains. Page errors and console errors are both zero. No further Travel Area deployment, migration, or formal acceptance is pending.
- 2026-09-04 Travel Area validation: all nine required mappings pass; live Google evidence passes 17/17. The focused Travel Area/Places/social-import release suite passes 39/39 in both canonical source and the deployment mirror. The earlier full canonical run remains 114/115 with only the pre-existing absent signed iOS Shortcut packaging failure; the deployment mirror remains 114/114 after excluding unrelated unfinished Shortcut UI/tests. Versioned assets are `20260904.1`.
- The complete pre-production formal-trip release gate also passed. A user-provided minimized snapshot of all 31 existing places was loaded through the unchanged new frontend and normal v5 `/api/places` migration path. Results were 31/31 resolved, zero failures, zero retry groups, 14 unique bilingual Travel Area groups, and zero page errors. Address-by-address review found no other incorrect grouping. That local acceptance did not mutate production; its snapshot and screenshots remain local-only evidence and are excluded from Git.
- Prior 2026-09-03 ward/municipality validation conclusions are revoked. In particular, Myeongdong → Jung-gu and Montmartre → Paris must never be treated as correct Travel Area results.

- 2026-09-03 manual-place release: `node --check app.js`, `node --check api/places.mjs`, and all 99 deployable `node --test` checks pass in both canonical source and deployment mirror. The canonical source's sole additional failure remains the pre-existing absent Apple-signed `shortcuts/旅伴匯入.shortcut`. A 393×852 browser layout pass measured a 342×767 editor sheet, 16px form controls, 44–54px actions, and no document-level horizontal overflow.

- 2026-09-02 manual Shopping photo release: the deploy mirror passes `node --check app.js` and 97/97 `node --test` tests. Coverage verifies the private photo kind, iPhone file picker, client compression, preview, replace/remove controls, 44px touch targets, manual-photo display priority, and the distinction from AI recognition screenshots. The canonical prototype also passes all 97 deployable tests; its sole additional failure is the pre-existing unfinished iOS Shortcut packaging assertion for the absent Apple-signed `.shortcut` file.
- `node --check app.js` passes.
- `node --test`: 95 tests passing as of 2026-08-27. Coverage includes the GET Web Share Target manifest/route, all five requested source hosts, deceptive-host rejection, session handoff into the existing confirmed import flow, Tabelog lookup initialization before startup backfill, persisted and exact known-restaurant Tabelog URLs, the separate fixed-size reservation action beside the phone card, official multilingual-App HTTPS Universal Links with exact deep-link and Traditional Chinese fallback parameters, fullscreen map structure, shared Shopping place classification across workflows, Booking's server-fetch anti-bot challenge, romanized Japanese house-number matching, multi-line host-message address/name extraction, URL-slug clues, real desktop popup return semantics, and accessible semantic bottom navigation. This validates the receiver logic but is not an iPhone share-sheet pass because WebKit does not expose manifest share targets.
- Responsive layout validation at 1280×800 and 393×852 confirms the fullscreen left rail, narrow-screen overlay drawer, fully expanded map, compact 34px markers, and safe-area floating controls fit without horizontal overflow or console errors. The gesture hint is offset below the fullscreen controls so the two never overlap.
- Browser validation at 375×812 confirms the four bottom buttons each measure about 89×58px, do not cause horizontal overflow, expose exactly one `aria-current="page"`, switch correctly across all four destinations, and produce no console warnings or errors.
- A reviewable handoff set now lives in `documentation/` (architecture, protected flows, permissions, variables, AI automation, and test coverage), with a Fable-oriented summary at `memory/FABLE_HANDOFF.md`.
- iPhone 15 Pro browser verification at 393×852 confirms the compact place-import sheet uses a 56px link field, keeps 16px form text, gives multi-candidate results a 316px independently scrollable region, and keeps confirmation actions fixed and visible while that region scrolls.
- 2026-08-07 audit: the canonical source, deployment mirror, and public Vercel assets remain aligned; core documented features showed no implementation drift.


## Ticket A — local implementation, 2026-09-07

- Scope: A1 Airbnb primary source notice, A2 nested photo-removal confirmation, A3 exclude place/lodging editor from global undo injection. Baseline HEAD and fetched origin/main both `fc13d4d483676f59e3620fbb422e7bcd43a7ca38`; worktree was clean.
- Source card/editor share `lodgingSourceStatusMessage`; approximate source precision remains in editor session. Address entry removes the missing-address notice and uses existing strict geocode statuses. Saved geocoded places retain the existing exact-location contract; candidate identity is unchanged.
- Photo removal reuses confirm-sheet as a sibling overlay, retains the editor DOM/session, makes the editor inert while confirming, supports keyboard cancel/focus return, and only marks photo touched/clears pending photo and photoOrigin on confirmation. Source name/URL are retained; persistence still requires Save. Existing stale/session/touched guards prevent late source images from returning.
- Global undo excludes only place-editor-form (including lodging); restoreLastAction and eligible screens are unchanged. Other form sheets (profile, trip/join, flight, transport, import, area/day assignment) may share the same misleading field-undo implication and were left outside this ticket.
- Validation: targeted 101/101; full regression 220/220, including unchanged startup 22/22; app.js and all 12 API modules pass syntax; API functions remain 12; git diff --check passes. Tests use the existing Node runtime harness; no production App or real-device visual test was opened.
- Local review only: no staging, commit, push, deployment, source-mirror copying, or edits to protected trip-deploy / p1-address-selection. Ticket B stays closed; no Ticket C or other deferred scope implemented.

## General place fallback and keyword search — 2026-09-08

- Baseline: latest origin/main a9ac1910aeb83f67b78c1873b7bc34697bc6f59f. Implemented in a new isolated worktree under travel-app/prototype/general-place-fallback; protected dirty worktrees and older AI家教 copies were not edited.
- Empty Google results now retain recognized source groups. Each group offers 搜尋其他地點 and 手動新增, including groups without any candidate. Recognition failures also offer keyword/manual recovery with an explicit unknown kind.
- Keyword search reuses /api/social-place-import action=rematch with searchMode=keyword. The submitted query is unchanged except boundary whitespace/length limits: no appended stale address/city/destination, NFKC conversion, regionCode, or destination locationBias. Google identity remains authoritative; previous Place IDs are excluded and only the target group is replaced. Existing automatic matching and legacy rematch behavior remain intact.
- Manual entry no longer unconditionally builds a lodging seed or overwrites the resolved source kind. Restaurant/attraction/shopping use general-place copy and their source kind; lodging retains its source/editor flow; unknown kind explicitly asks for 景點 / 餐廳 / 住宿 / 購物. Manual seeds contain source name/address/reference only, never a rejected candidate's Place ID or coordinates.
- Validation: targeted 139/139 and full regression 329/329 passed. Final manual-entry handler correction has a new runtime regression test; affected fallback/lodging targeted suite passes 38/38. app.js and all 12 API files pass syntax; git diff --check passes. Local Edge headless 393x852 rendering confirms four editor kinds without horizontal overflow and all four unknown-kind choices. This is local mocked UI validation, not live Instagram/Google or production acceptance.
- Assets: 20260908.2. API functions: 12. No endpoint/schema migration, Shopping feature, restaurantTags, Travel Area rules, identity enrichment, or selection-mode changes.
- Production acceptance is reserved for the user: paste the Instagram restaurant post, use automatic candidates or 搜尋其他地點, enter the intended store/branch, inspect/select/import results; search an absent name then 手動新增 and verify kind/address/source. Test a lodging URL and unknown-kind manual entry as well. Production App was not opened by the agent.


## Places area and restaurant filters — 2026-09-08

- Built from origin/main fab6a1acbf10ab2d156775de4985c5706529e315 in isolated canonical-source worktree travel-app/prototype/places-filters. Existing dirty workspaces are preserved.
- Places list has compact horizontally scrolling area chips, dynamically derived from this trip's stored travelAreaKey/travelAreaZh. Filtering compares keys only; no resolver/API request or Travel Area rule change. Missing/unclassified areas remain visible under All.
- Restaurant-only cuisine chips use the trip's actual restaurantTags. Area and cuisine combine with kind using AND; invalid selections reset to All. Cuisine selection applies across top-level kinds; see UX follow-up. Filter state is device UI only and resets when trip view clears.
- Optional restaurantTags: string[] supports multiple values and 13 default editor choices plus retained custom values. Existing missing fields need no migration. Explicit arrays, including manual clearing to [], are preserved. Old restaurant detail refresh never adds tags; new imports with exact known Google response categories can conservatively supply tags; names and AI classification are never used. Existing sharedTripPayload/cleanTrip/JSON reload preserve fields without server changes.
- Validation: targeted 143/143; full regression once 337/337; app.js and all 12 API modules pass syntax; git diff --check passes. Local Edge headless 393x852 has zero horizontal document overflow, two 48px scrolling filter rows, 341px editor, 18px checkboxes and working multi-selection. API functions remain 12; assets 20260908.3.
- Scope excluded: Aidaya/existing-candidate exclusion, Shopping/Threads changes, lodging matching/source, P0 identity, Travel Area resolver, and candidate-editor refactoring. Production App was not opened; user owns acceptance.
- Acceptance: choose restaurant, edit a place and select multiple cuisine tags, save/reload/reopen editor; choose area + cuisine and verify intersection, switch All to include untagged/unclassified places, clear the only occurrence of a selected tag to confirm filter resets. Swipe the filter rows on iPhone.


## Places filter UX follow-up — 2026-09-08

- Baseline origin/main e7f1ad5; isolated canonical worktree travel-app/prototype/places-filter-ux; deployment mirror places-filters-deploy.
- Area/cuisine chips have bounded flex width, nowrap, native touch panning, momentum scrolling, hidden scrollbars and contained horizontal overscroll. Selection retains row scroll offset.
- List and both map layouts share placeAreaFilter (stored Travel Area key). Map chips change the same selection; map results and fitBounds reflect matching located places. No located results preserve the previous center/zoom, scoped to the trip. Area changes clear the selected map-place preview, and live-location centering does not override a selected area.
- Existing resolver catalog contains area key/names/aliases and Google integration supplies point coordinates/address components. No trustworthy area polygon exists. No inferred boundary drawn; area boundary needs an additional geometry source.
- Fullscreen action moved from the normal map title into an operation row beside Show location; fullscreen behavior retained. Both controls have 44px height.
- Cuisine row appears whenever any restaurant in the trip has tags, including top-level All. Choosing a tag sets top-level All and matches only tagged restaurants, AND area. Untagged restaurants match cuisine All. Old-restaurant detail enrichment no longer populates tags; schema/editor/persistence and new-import category mapping unchanged.
- Validation: targeted 107/107; full regression once 340/340; app.js and 12 API modules syntax pass; git diff --check pass; API functions 12. Full shipped local DOM/Leaflet at 393x852: native touch moved chips 225px, last chip fully reachable, cuisine row scrolls, List-to-Map area count 2, empty viewport preserved, controls same y=345, fullscreen selection retained, no document overflow or page errors. No production App opened. Assets 20260908.4.


## Fullscreen, inferred cuisine and sourced area boundaries — 2026-09-09

- Baseline 1af5839, isolated canonical source travel-app/prototype/area-geometry; validated deployment mirror places-filters-deploy.
- Fullscreen uses a compact dynamic area select keyed by travelAreaKey. A single always-visible 44x64 left-center arrow toggles the sliding drawer, updates aria-expanded/inert and preserves the live map instance. No X or filter-icon opener remains; exit fullscreen is independent. Normal list/map chips remain unchanged. Area and cuisine selection persist across list/map/fullscreen; map now honors selected cuisine too.
- Restaurants without an explicit restaurantTags array derive tags at runtime from clear category/source/tags/description/name cuisine terms (Chinese/Japanese/English). Unknown evidence stays empty; no AI/API calls or persistence backfill. Existing arrays including [] always override inference. Editor displays inferred checks and saves restaurantTagsSource=manual alongside selected values, including explicit clearing; old payloads stay compatible with no migration.
- Trustworthy geometry now ships as a same-origin static OSM/ODbL snapshot for 12 Tokyo area mappings: Ginza; Ebisu/Daikanyama; Shibuya; Asakusa; Shinjuku; Otsuka; Ikebukuro; Toyosu; Marunouchi/Otemachi; Tsukiji; Azabujuban; Ueno. Exact source relation IDs, names, tags, timestamp, URLs, license and raw snapshot hash are preserved in data/area-geometry; reproducible offline builder joins only original way endpoints. Ebisu/Daikanyama keeps three separate Ebisu, Ebisu-Nishi and Daikanyamacho polygons. Other compound areas likewise keep separate authentic components. These OSM town boundaries are not an invented official perimeter for an informal travel area.
- Google renders non-clickable dashed closed paths; Leaflet uses unfilled dashed GeoJSON in a pointer-transparent noninteractive pane. Visible OSM/ODbL attribution accompanies boundaries. Geometry loads asynchronously from one cached local asset promise; markers/filter/fitBounds do not wait. Missing geometry draws nothing; All clears layers. Map/trip/selection tokens prevent late responses from painting stale regions. No production Overpass/Nominatim requests or extra function; missing-area viewport behavior retained.
- Validation: targeted 141/141; full regression once 348/348; app.js and all 12 API modules syntax pass; git diff --check pass; API functions 12. Local full-DOM/Leaflet touch fixture at 393x852 passes dropdown, arrow open/close retaining map, three composite polygons, absent/All boundary removal, noninteractive pane, List/Map/fullscreen area+cuisine sync, inferred editor checks, zero page errors/overflow. Production App not opened. Assets 20260909.1.
- Deferred/protected: Aidaya keyword exclusion, Travel Area resolver identity, Google identity, social/lodging/Shopping routing and import selection modes unchanged.


## Production UX corrections — 2026-09-09

- Baseline fac25f6; isolated canonical worktree travel-app/prototype/places-production-ux. Validated files copied to clean places-filters-deploy mirror.
- Fullscreen drawer handle now sits at the panel right edge while open, moves to viewport left while closed, and follows the slide transition. Visible tab 22px wide inside a 44x64px touch target; panel padding restored. Independent fullscreen exit and map gestures retained.
- Both Google and Leaflet boundary stroke reuse computed --accent (#c8452d), still dashed, unfilled and noninteractive. OSM geometry/source files are unchanged.
- Filter/editor heading is 類別. Available tags derive only from restaurants in the selected travelAreaKey; All area includes the whole trip. Area changes reset a category absent from the new area, then apply area/category/kind AND filters.
- Restaurant details have a visible 編輯類別 entry above the gallery, opening the existing general editor. 13 defaults/custom values remain multi-select. Existing restaurants can 儲存類別 independently; tag-only primary Save uses the same path when all other form values match their opening snapshot. This changes only restaurantTags and restaurantTagsSource, preserves explicit [] and all identity/address/photos, and needs no geocode. Other edits retain the existing form path.
- placeMapsUrl retains exact Place ID first and trustworthy CID/place-page URLs before coordinate-only fallback for ordinary POIs; navigation paths/parameters are excluded from place-page output. Custom/address identities retain their coordinate behavior. Google gallery photos link to the place page while img src remains the unchanged identitySafePhotos-guarded /api/place-photo resource. Link clicks prevent default duplicate navigation. Explicit navigation action still uses placeNavigationUrl (coordinates then address); null/empty coordinate values are no longer coerced to zero. No name re-search or Google identity/photo-source rewrite.
- Validation: targeted 132/132; full regression once 352/352; app.js/all 12 API modules syntax pass; git diff --check pass; API 12. Local 393x852 full-DOM/Leaflet: open handle x=300 matches panel right=300, visual width22/touch44, collapse left correct, stroke #c8452d, regional tags/reset, visible editor entry, multi-select/save/JSON reload/explicit clear, original identity/address/photos preserved, exact place-page photo link; no page errors/overflow. Production App not opened. Asset version 20260909.2.

## Places photo and category cleanup — 2026-09-09

- Baseline 0684223; canonical isolated source travel-app/prototype/places-cleanup. No production App opened.
- Detail gallery previously rendered stored custom/source media and the first three exact Google photo references without image-error handling. Failed remote media or /api/place-photo non-image error responses therefore left browser broken-image icons. The new per-gallery queue removes failed images, consumes unused valid photo resources belonging to the same exact Place ID, then shows an App placeholder. Pending images remain hidden until decoded; cached completions are handled. Neither search nor saved place/identity/photo metadata is changed. Place-page links and navigation remain separate. This diagnosis was reproduced locally; no claim of production-store-specific HTTP tracing.
- Restaurant details directly show category chips; the separate edit-category action and Save categories button are removed. The existing bottom general editor and its single Save remain. Tag-only saves retain the existing safe path preserving all non-tag fields.
- Editor chips derive from current trip restaurant values plus the current restaurant's own tags, with no fixed 13-item suggestion list. Custom labels trim, reject empty strings and deduplicate exact text; unchecking removes an assignment and clearing all saves explicit []. An unused tag disappears after save/reload; no global category database or schema migration. Explicit/manual arrays always win. Inference uses existing category/source/tags/description evidence, excludes names alone, and recognizes explicit seafood-buffet terms.
- Filters remain area-scoped using travelAreaKey and area/category AND. Unavailable selections reset, empty category rows hide. Secondary chips use 12px text, 11px horizontal padding and 32px visible pills inside 44px touch targets with native horizontal scrolling.
- Boundaries now use solid --accent #c8452d strokes for both map providers, unfilled and noninteractive. OSM geometry/source/attribution and composite components unchanged. API functions remain 12.
- Validation: targeted 134/134; full regression once 354/354; syntax of app.js and all 12 API modules; git diff --check. scripts/verify-places-ux.cjs runs the full app and Leaflet locally in a real touch-enabled browser at 393x852, with controlled image HTTP responses: partial and total photo failure, wrong-identity exclusion, real image decoding, custom tag add/dedup/remove/save/JSON reload/clear, dynamic options, solid boundary, drawer placement, secondary 44px touch area, zero page errors/overflow. Run with PLAYWRIGHT_MODULE set to an installed Playwright package. Assets 20260909.3.

## Exact photo resource refresh — 2026-09-09

- User accepted other UX; screenshots showed Ginza Hachigo working while three older entries had placeholders. Prior fallback only consumed saved references; photosLoaded/detailsLocked prevented normal detail updates. Google documents that photo resource names expire and must be obtained from recent Place Details (https://developers.google.com/maps/documentation/places/web-service/place-photos). No production App or private trip store opened; affected stores' individual HTTP failures were not inspected.
- Existing /api/place-photo now accepts exact placeId to fetch only id,photos, rejects mismatched response IDs, filters matching photo resource prefixes and returns at most 10 candidates with attribution. No text search. JSON and media redirects use no-store; no new function.
- Each detail gallery can refresh once when an image fails or saved safe references are insufficient, regardless of photosLoaded/detailsLocked. Fresh resources stay transient; no saved identity/name/address/tags/photos are mutated. Detached galleries and mismatched IDs ignore late responses. Refreshed image failures consume remaining exact candidates then use placeholders without retry loops. API/network failure retains existing safe images/placeholders.
- Validation: targeted 13/13; full regression once 356/356; syntax and git diff --check passed; API functions 12. Real local browser scripts/verify-photo-refresh.cjs recovers decoded fresh photos from expired references with photosLoaded/detailsLocked true, verifies one refresh/no metadata mutation and empty/mismatched/offline graceful fallback. Production App not opened. Assets 20260909.4.

## Fullscreen drawer filters — 2026-09-09

- Requested baseline 4d72a50; fetch confirmed latest origin/main d7d2863, preserving accepted photo refresh. Canonical worktree travel-app/prototype/drawer-filters.
- Fullscreen removes the duplicate kind dropdown while retaining top kind buttons; ordinary map dropdown unchanged. Restaurant drawer displays category chips through existing placesFilterModel/placesFilterChips, with area:false so the compact area dropdown remains unique. Options use actual selected-area restaurant tags and existing invalid-tag reset.
- List/Map/fullscreen share placeKind/placeAreaFilter/restaurantTagFilter/mapPreference. Mode switching no longer resets filters. Picking a restaurant tag retains restaurant kind when already selected. List applies existing matchesMapFilters so retained preference affects results too; date retains its existing day-map semantics.
- No changes to photo fallback, editor/custom categories, geometry/boundary, drawer handle, URL helpers, AI, Shopping or lodging.
- Targeted 88/88; full regression once 357/357; syntax and diff checks passed; API functions 12. Local real browser at 393x852 passes no duplicate kind, area-scoped drawer categories/reset, single-place AND result and shared kind/area/tag/preference across List/Map/fullscreen round trips. Production App not opened. Assets 20260909.5.

## Travel Area boundary component semantics — 2026-09-09

- Baseline 7fee1ed; canonical source travel-app/prototype/boundary-components. Existing polygons were already separate, but uniform unlabeled rendering implied one Travel Area administrative boundary. No Google/OSM source equivalence is claimed.
- Runtime mapping explicitly exposes travelAreaKey -> geometryComponents[] backed by original FeatureCollection features. Every component keeps its original rings, OSM relation ID and local name. No geometry files, resolver, union/hull/outer envelope or coordinates changed.
- Google draws each component's original rings with componentId and uses small OverlayView text in overlayLayer below POI markers. Leaflet creates independent GeoJSON component layers and noninteractive permanent labels in its pointer-transparent boundary pane. Labels anchor to a vertex on the original outer ring (text positioning only). Existing accent solid strokes retained.
- Credit says N independent town boundaries for composites, and explains lines are source town boundaries rather than an official Travel Area administrative perimeter. OSM/ODbL credit remains. Single component renders normally; All clears boundaries and label overlays. Existing async stale-response protection retained.
- Ebisu/Daikanyama components: 恵比寿 (relation 9521529), 恵比寿西 (17008303), 代官山町 (17022574). All 12 catalog mappings preserve the original feature/coordinate data.
- Validation: targeted 10/10; full regression once 359/359; app/API syntax, diff check; API functions 12. Local full-DOM touch browser validates three separate Leaflet layers/local-name labels, pointer-events:none, fullscreen switch to one Ginza component, then All removes every line/label/credit. Production App not opened. Assets 20260909.6.

## Travel Area geometry audit — 2026-09-09

- Audited 61 finite catalog entries: 13 verified core mappings, 48 explicit unavailable. See documentation/travel-area-geometry-audit.md and data/area-geometry/mapping-audit.json for IDs, names, coverage and official source evidence. OSM/ODbL attribution and both raw snapshots preserved.
- Split Ebisu / Daikanyama. Evidence/address/verified containment only; Ebisu-Nishi ambiguity remains existing value. GET conversion is non-persistent; normal save persists. No name lookup or P0 placeId/address/photo edits. Online trip API returned 401: actual reclassification/unresolved counts unavailable.
- Shinjuku adds Kabukicho, drops whole Yoyogi; Ueno adds park; Shibuya adds Udagawacho/Dogenzaka/Maruyamacho, drops whole Jingumae/Tomigaya polygon coverage; Ikebukuro adds Minami-Ikebukuro. Internal components stay independent, one user-facing label per area. Orange solid noninteractive lines, partial core coverage.
- Validation: targeted 41/41; one full regression 365/365; local touch/mobile Edge all 13 mappings and All clearing passed; syntax/diff pass; API functions 12. Production App not opened.

## Travel Area boundary union — 2026-09-10 (current; supersedes component rendering)

- Canonical source: travel-app/prototype/boundary-union, continuing the existing working state from origin/main 00a4a52. Validated deployment mirror: existing places-filters-deploy worktree of trip-deploy. No worktree recreated and no reset/stash/clean.
- Complete baseline 61/61 audit maps to 63 final areas. Ebisu/Daikanyama remain independent; Harajuku/Omotesando and Tokyo Tower/Shiba Park now have independent keys. Marunouchi/Otemachi intentionally remains a reviewed adjacent business-district composite; internal towns never become filters.
- Sources: 10 U.S. Census official boundaries, 44 OSM/ODbL boundaries, zero Google DDS, 9 absent. Absent: tokyo-tower, harajuku, omotesando, nerima, myeongdong, hongdae, itaewon, gangnam, montmartre. Source availability and semantic/qualification limitations are explicit per area in documentation/travel-area-boundary-audit.md and data/area-geometry/boundary-audit.json. Existing Google DDS findings retained: supported national feature types do not establish specific travel-circle features; app has no configured DDS vector map ID or reviewed exact feature Place IDs. No consumer scraping or polygon extraction.
- Offline Shapely 2.1.2 / GEOS unary_union creates the static travel-area-boundaries.json contract from saved inputs. No hull/buffer/snap/repair/artificial connectors. Both Google and Leaflet render only each final polygon exterior with accent solid noninteractive polylines; final holes remain available solely for containment. Boundary fit includes saved markers, All clears, stale map/trip/filter tokens are guarded, and Leaflet boundary fit disables animation to prevent disposed-map callbacks on rapid switching. Correct source-specific attribution. Raw OSM snapshots and derived OSM geometry retain ODbL, Census remains separate public-domain data.
- Safe resolver handles all three legacy pairs using saved explicit area evidence, locality/address and verified final containment. Ambiguous/conflicting/boundary/name-only records stay intact and display 地區待確認; no automatic Google lookup for unresolved split aliases. GET does not persist; ordinary authorized save can persist resolved fields. All non-area fields and Google identity remain unchanged, with original area fields preserved in provenance. Actual production converted/unresolved counts unknown because no authenticated inventory is available; no invented zero counts or production data changes.
- Validation: final targeted suite 110/110; full regression run once, 375/375; app, library and all 12 API syntax; git diff --check; API functions exactly 12. Offline topology gate verifies all 54 true unions against saved source geometry and 9 empty entries, shared-edge disappearance, islands, holes and invalid-input rejection. Local mobile browser verifies all 63 selections, exact 54 exterior sets, 9 empty boundaries, attribution, noninteractivity, pan, rapid switches and All clearing; zero page errors. No production App opened. Frontend assets 20260910.1.
- Release: validated; commit/push/production READY recorded after deployment confirmation.


## Optional areaTags experiment — 2026-09-10

- Baseline: fetched latest origin/main 4d778b5c3556a99a510c5b38664c0683d0132d2f and created new detached clean worktree area-tags-experiment. Per explicit user instruction, all implementation and memory changes stay here; no prototype/mirror sync, reset/stash/clean or edits to existing worktrees.
- Optional areaTags string array is independent of every legacy Travel Area field. Missing remains absent, empty is valid; trim/nonempty/Unicode-case dedupe preserves display text. No catalog or geography requirement. Existing general editor now has removable multi-tag chips, one-click current-trip tags, literal saved-address suggestions and custom input. Suggestions are draft UI only until user selection and Save.
- Conservative address components (neighborhood/sublocality/locality) take priority; numeric 丁目 suffix stripping and narrowly qualified formatted-address fallback only. No semantic aliases, Google lookup, polygon inference, global learning or automatic migration. Detail omits empty tag rows.
- Shared List/Map/fullscreen state areaTagFilter derives options only from active-trip saved tags. Multi-tag records match each tag, empty records remain in All. Tag redraw skips legacy background split/coordinate enrichment; legacy area filter/boundary retains its existing behavior. Legacy resolver and complete boundary loader/exterior code, catalogs and geometry/build inputs are unchanged.
- Tag-only editor save preserves exact Place identity/address/photos and all other fields; optional simultaneous restaurant tag edits are also retained. Standard save and real startup serialization/hydration retain user labels. API sanitizes explicit tags; omitted older-client fields preserve tags only on a unique stable identity; explicit [] clears. No new endpoint: API functions 12.
- Validation: final targeted 122/122; full regression run once 393/393. Automated editor/API/startup round trips, legacy success/failure, filter and boundary isolation pass. No browser smoke test or production App access, as requested. Protected snapshot checks all 1,805 existing files unchanged with no added/missing files. Frontend assets 20260910.2 (legacy audit asset retained at .1).
- Scope/acceptance details: documentation/area-tags-experiment.md. User will perform production comparison and decide any future alias work. Publish this isolated worktree after syntax/diff checks and fast-forward verification; release status belongs to the final deployment confirmation.


## areaTags address suggestions targeted correction — 2026-09-10

- Continue the clean isolated area-tags-experiment worktree at deployed 8c24e9f. Only suggestion helpers, their asset version and regression/documentation change; no app editor/filter/persistence/API/legacy resolver/taxonomy/boundary implementation edits.
- Root cause: locality was positively included, and numeric chome exclusion did not fold macrons or Unicode hyphens. Explicit administrative/locality/postal/street/country exclusions now override positive types; Japanese sublocality level 1 and explicit city/ward names are excluded. Numeric 丁目/chōme/chome/番/號 patterns use normalized comparison, leaving persisted user display values untouched.
- Localization reads only paired saved addressComponents/addressComponentsOriginal. Prefer current localized text; a Latin candidate may use a unique exact component-type counterpart with local script. Duplicate/missing counterparts retain the source name. No index matching, legacy area/travelArea fields, translation table or semantic aliases.
- Production-case fixtures: Chuo City/Ginza/8-chōme -> Ginza or saved 銀座; Shibuya/Ebisunishi/2-chōme -> Ebisunishi or saved 恵比寿西; Minato City/Shiba/3-chōme -> Shiba or saved 芝, never legacy 港. Suggestions do not persist automatically; manual values remain independent.
- Targeted 93/93; full regression once 398/398. API remains 12. No production App/browser smoke test. area-tags.js asset version 20260910.3; other assets unchanged. Publish after syntax/diff and fast-forward checks.


## areaTags UI acceptance polish — 2026-09-10

- Continue isolated area-tags-experiment from e7ce702. No suggestion/localization/model/API/legacy resolver/boundary changes. No hydration fix: the reported Ginza images were an in-page selection toggle.
- Area selected chips share the category chip base and selected CSS rule (#315c50 solid / white). Selected container reserves 44px when empty; normal one-row toggles preserve input/address placement, with flex wrapping allowed for multiple rows.
- Editor order: title -> selected container -> input/add -> absolutely positioned autocomplete -> address. Trip-used options require nonempty matching text and never show the whole trip on initial focus. Focus without text can show existing address suggestions. Selected labels are excluded by the unchanged AreaTags module; overlay open/close does not contribute layout height. Escape/blur dismiss, arrow keys/Enter and touch select without saving before editor confirmation.
- Detail source inventory: old title kicker was travelAreaDisplayName(place); new 地區 row is areaTags; byline is fullName/name + category; restaurant section is restaurantTagValues; description is place.description; highlights are place.highlights. Only the old kicker moves to small muted 舊分區 below primary areaTags. Description/category/highlights and saved fields remain intact.
- iOS up/down/checkmark inferred native form accessory from source review (editor renders no such bar) and Apple's Designing Forms documentation describing Previous/Next/Done. No inputmode=none, forced blur, viewport or native keyboard workaround. No production App visit or browser smoke test.
- Targeted 85/85; one full regression 403/403. Tests cover stable reserved container/absolute overlay CSS contracts, control-node persistence, empty/focus/query/selected option behavior, keyboard/IME/touch actions, detail source separation and shared selected style. API functions 12; app/styles assets 20260910.4, suggestion library remains 20260910.3. Deployment follows syntax/diff/fast-forward gates.


## Address suggestion decoupling and English chome fallback — 2026-09-10

- Continue isolated area-tags-experiment. Fixed two reported bugs: saved-address suggestions were shown only inside the focus-triggered trip-tag autocomplete, and the formatted-address fallback matched Japanese 丁目 text but not an English Booking-style `1 Chome - 16 - 19 Okubo, Shinjuku - ku, Tōkyō - to 169 - 0072` address.
- Address suggestions render in their own persistent row independent of the trip-tag input's focus/query state; the autocomplete dropdown now carries only trip-used labels and still needs a nonempty query. `formattedCandidates` adds a Latin chōme/chome-block-plus-municipality-delimiter match (forward and reverse word order) over a parser-only normalized copy; room numbers, the block number, prefecture/postal text and bare building names never qualify because the district must sit directly between the block and an explicit `City`/`-ku`/`-shi` token. The persisted address string is never rewritten.
- Added a working-identity-only romanization fold (`romanKey`): vowel macrons (ā/ē/ī/ō/ū) collapse to their bare vowel solely inside a per-render Japanese-address `comparison(places)` context, so `Ōkubo`/`Okubo` share one dedupe/filter identity. No other accent folds (Café/Cafe, Mâcon/Macon stay distinct) and non-Japanese places are unaffected. Display label precedence: existing persisted label in the group > reliable exact localized counterpart evidenced by paired addressComponents/addressComponentsOriginal > original romanized candidate; comparison normalization never rewrites a persisted label or source address. No semantic/travel-area alias added; 大久保/Ōkubo/Okubo never suggests 新大久保, and existing Ebisunishi/Jingumae/Sotokanda/Shiba/Nishishinjuku fixtures still forbid their neighborhood nicknames.
- Targeted area-tags 20/20, lodging-editor 46/46; full canonical suite 409/409. No API/persistence/legacy Travel Area/geometry file touched. Assets advanced to 20260910.5. No production App or browser smoke test this round.


## Booking suggestion integration fix and detail chip-row merge — 2026-09-10

- Real production Booking lodging ("自由之家", `Room 202 , 1 Chome - 16 - 19 Okubo / Shinjuku - ku, Tōkyō - to 169 - 0072`) resolved Travel Area to 新宿 correctly but showed no areaTags suggestion. Root cause was NOT the parser (re-verified correct in isolation) but the lodging-editor integration: suggestions were computed once from a bind-time snapshot (`session.areaTagSource`) that never refreshed when the address changed via async Booking-draft recognition (`fillPlaceEditorFromUrl`'s direct DOM value assignment, no `"input"` event) or via the existing generic address `"input"` handler (which only drove geocode debounce, never suggestions).
- Fix: `areaTagAddressSource(form)` in `app.js` compares the live address textarea against `session.areaTagBaseline` (captured at editor-open); unchanged reuses the bound structured evidence, changed uses only the current typed/pasted text (dropping stale structured components). `renderAreaTagDraft` now derives suggestions from this live source every render and is invoked from the address `"input"` listener, `fillPlaceEditorFromUrl`, and `syncSource`'s address-clear. `lib/area-tags.js` was not touched this round.
- Added a real end-to-end integration regression (`tests/lodging-editor.test.mjs`) driving `bindPlaceEditor`/`fillPlaceEditorFromUrl`/submit for the exact reported address, proving the earlier unit-only `addressSuggestions()` coverage was insufficient on its own.
- Detail sheet: merged the separate areaTags `地區：` row and restaurant `類別` row into one wrapping chip row (`placeTagsDetail`, areaTags first then restaurant category chips), no label text, no invented empty-category placeholder, same `.highlight-tag` visual style and `.detail-area-tags` container/CSS. Legacy `舊分區：` line, filters, editor and persistence unchanged.
- `romanKey` call sites re-audited: unchanged, still scoped to per-render Japanese-address comparison only; no global accent folding, no persisted/display rewrite, no semantic alias.
- Targeted area-tags 20/20, lodging-editor 47/47, place-identity 13/13; full regression once 411/411. API remains 12. app.js assets advanced to 20260910.6 (lib/area-tags.js, styles.css unchanged this round). No production App or browser smoke test this round.


## Sticky save footer and same-place Save/Cancel navigation — 2026-09-11

- The Place/Lodging editor's `.modal-actions` (Cancel/Save) is now `position: sticky; bottom: 0` inside the editor's own scrolling `.place-editor-sheet` (not viewport-fixed, so it survives iOS keyboard/accessory-bar viewport compression), always visible while bulk-editing areaTags without scrolling to the form's end.
- Save (all three paths: full submit, `saveAreaTagsOnly`, `saveRestaurantTagsOnly`) and Cancel (button and backdrop dismiss) on an EXISTING place now return to that same place's detail sheet instead of the Places list. `bindPlaceEditor` captures `session.detailReturnKey = placeDetailKey(existing)` (stable P0 identity: id > placeId > sourceUrl > name) at open time; Save reopens via the freshly saved record's own key (handles rename), Cancel reopens the untouched original (the draft was never written back). `resolveDetailPlace`/`openPlaceSheet` always search full `state.places`, never a filtered subset, so this works even if the save makes the place stop matching the active filter. An existing-place save/cancel no longer resets `state.placeKind`/`state.selectedArea`/any filter, and Cancel never calls `render()` at all -- the underlying list's scroll and filter state are simply untouched. Creating a brand-new place is unchanged (closes to list, still switches the kind filter to show it, since there is no prior detail to return to).
- New `tests/place-editor-navigation.test.mjs` (6 tests) exercises the real save/cancel/detail-reopen pipeline end-to-end plus a sticky-footer CSS contract check.
- Targeted lodging-editor 47/47, place-editor-navigation 6/6; full regression once 417/417. API remains 12. Frontend assets advanced to 20260911.1. No production App or browser smoke test this round.

## Candidate confirmation draft editor and batch add — 2026-09-11

- Candidate confirmation page (`openImportCandidatePreview`) now has a 編輯 text-button next to the × close button (not in the bottom action row), and its bottom action row is fixed to `[返回候選] [確認選取]` -- the old "取消選取"/"加入這個地點" toggle button is gone. The page always renders a candidate's saved draft merged over its original data when one exists (`{...original, ...draft}`), so edits are visible immediately on return without needing a re-render trigger elsewhere.
- 編輯 opens the existing place editor (`openPlaceEditSheet`) with a new explicit `seed.editorMode: "candidate-draft"` + `seed.candidateIdentity`, which the form carries as `data-editor-mode`/`data-candidate-identity`. `bindPlaceEditor`, `bindAreaTagEditor`, the area-tag suggestion pipeline, the photo picker, and the restaurant-tag editor are all reused completely unchanged (only the submit handler branches early on `data-editor-mode === "candidate-draft"` into the new `submitCandidateDraftEditor`, before any of the existing tag-only-save fast paths or the full `nextPlace`-building/geocode path run).
- `submitCandidateDraftEditor` never calls `/api/places`, `persist()`, or writes to `state.places`; it commits `name`/`kind`/`category`/`restaurantTags`/`restaurantTagsSource`/`areaTags`/`customPhotoDataUrl`/`photoOrigin` onto the candidate's entry in a new module-level `candidateDraftStore` (`Map<identity, draft>`), then calls the new `reopenImportCandidateSheet` to rebuild the import list sheet (via factored-out `importPlacesSheetMarkup`) and reopen the same candidate's confirmation page. Cancel (the shared close/dismiss handler) discards the unsaved form state and does the same reopen, without touching the store.
- Batch add (`import-places-form` submit, still the only `state.places` writer) now maps each submittable candidate through `finalizeCandidateForBatchAdd` before the existing bookkeeping-field strip: it overlays any saved draft's edited fields, then force-restores a fixed Google-identity/evidence field list from the original candidate so a draft can never carry stale/corrupted identity into a real place. `endImportSession()` (clears `candidateDraftStore`) runs on batch-add success and on the import sheet's own close/cancel (detected via `#import-places-form` inside the shared close/dismiss handler); it deliberately does not run on candidate-list navigation, preview close, or unchecking a candidate.
- New `tests/candidate-draft.test.mjs` (18 tests, lettered A-R matching the spec's regression list) builds a VM harness that loads the real `bindPlaceEditor`/`submitCandidateDraftEditor`/`selectImportCandidate`/`submittablePlaceImports`/`finalizeCandidateForBatchAdd`/`endImportSession` source directly from `app.js` (same technique as `tests/lodging-editor.test.mjs`) plus the real `AreaTags` module and the real `data/area-geometry/travel-area-boundaries.json` catalog for the two suggestion tests (verified Ginza containment hit, no-semantic-guess Jingumae address fallback, using the same coordinates as `fallbackPlaces`' "銀座八芳").
- Three pre-existing test files needed small updates to stay accurate for the new behavior, not because of a regression: `tests/ui-logic.test.mjs` (one assertion updated from `data-select-import-candidate` to `data-confirm-import-candidate`, matching the deliberate button rename), and `tests/maps-text-import.test.mjs`/`tests/lodging-editor.test.mjs` (both extract and `vm.runInContext` the real `import-places-form` submit handler source, which now calls `finalizeCandidateForBatchAdd`; their VM contexts needed that function plus `candidateDraftStore`/`CANDIDATE_DRAFT_IDENTITY_FIELDS`/`endImportSession` stubs/sources added, same pattern already used for every other extracted helper in those harnesses).
- Validation: targeted ui-logic 67/67, social-place-import 35/35, place-editor-navigation 6/6, place-identity 13/13, area-tag-containment 9/9, lodging-editor 47/47, maps-text-import 15/15, places 6/6, places-filters 13/13, candidate-draft 18/18; one full regression run across all 29 test files 443/444. The sole failure (`tests/area-tags.test.mjs`, "tag selection does not invoke the boundary loader...") is a pre-existing CRLF-vs-LF string-marker bug in that test's own `section()` helper (`indexOf` of an `\n`-only marker against this Windows worktree's `\r\n` `app.js`, `core.autocrlf=true`), reproduced identically by running the same test against `git show HEAD:app.js` written to disk normally (i.e. before this session's diff existed) -- confirmed pre-existing and out of scope; left untouched. app.js/styles.css/all 12 API modules syntax pass; `git diff --check` passes; API functions remain exactly 12 (no `api/` files touched). Frontend assets: app.js `20260911.2` -> `20260911.3`, styles.css `20260911.1` -> `20260911.2` via index.html; `lib/area-tags.js`/`lib/travel-area-audit.js` untouched.
- Scope discipline: `lib/area-tags.js` (areaTags data model + containment/suggestion algorithm), the formatted-address parser, Travel Area/boundary geometry, existing Google `placeId`/identity anti-pollution logic, the `api/` directory, and the existing persisted-place editor's own form fields/save/cancel/navigation contract were not touched -- verified both by design (candidate-draft mode is an early branch that exits before any of that code runs) and by the full lodging-editor.test.mjs/place-editor-navigation.test.mjs suites passing unchanged.
- No production App opened this round; user owns device acceptance of the new 編輯/確認選取 flow.


## Candidate initial tags and Places cards — 2026-09-11

- Source: travel-app/prototype/candidate-tags, based on fetched origin/main f544f80. Protected worktrees and dirty deployment checkout preserved.
- candidateDraft creates a session Map entry once per importCandidateIdentity. Existing nonempty area tags are retained; otherwise AreaTags.suggestions(draft, state.places, [], areaGeometryCatalog).address supplies the first valid default. Both initial analysis and rematch intake await the existing cached local geometry before draft creation. No new resolver, geometry, alias, network service or schema.
- Restaurant defaults reuse production restaurantTagValues / inferredRestaurantTags (category/sourceCategory/googleCategory/tags/description evidence, never name-only). Explicit arrays including [] win; unknown evidence stays empty. No new classifier and category itself is preserved.
- Saved manual edit/clear wins because Map.has, never tag length, is the initialization guard. Confirm/selection/render/reopen/batch-add cannot replace a committed empty array. Original identity remains authoritative and batch add remains the only places persistence path.
- Candidate confirmation uses committed draft + placeTagsDetail. Places cards use persisted arrays + placeTagsList, area before cuisine in a wrapping row; missing tags omit the row. Cards cap tag-row height at 76px, and full details remain available. No legacy Travel Area or inferred parser tokens in chip rows. Existing filter/sort/editor contracts unchanged.
- Validation: targeted 199/200, only the known Windows CRLF source-slicing failure in area-tags.test.mjs. Full regression executed once: 443/446; two rematch harnesses lacked the new dependencies and were then updated; their two files re-run 51/51. Thus no unresolved new failure; known CRLF failure remains. area-tags.test.mjs, .gitattributes, core.autocrlf and checkout line endings were not normalized or changed. JS syntax and diff checks pass, API modules exactly 12.
- Browser integration: scripts/verify-candidate-tags.cjs runs the full app in local Edge at 393x852 with local HTTP fixtures. Delegated candidate click -> openImportCandidatePreview; delegated edit click -> openImportCandidateEditor -> openPlaceEditSheet -> bindPlaceEditor; actual form submit listener -> submitCandidateDraftEditor -> reopenImportCandidateSheet -> openImportCandidatePreview. Also actual Cancel, clear/reopen, selection, batch submit -> state.places -> render -> placesScreen -> card DOM; rematch click -> rematchImportCandidateGroup -> local HTTP response -> geometry load -> candidateDraft -> preview list, before any candidate detail opens. Persistence I/O is stubbed; no product handlers/rendering are replaced. Wrap/height/overflow and no page errors verified.
- Helper-level coverage remains in candidate-draft.test.mjs: containment Ginza, Jingumae fallback (never Harajuku), cuisine evidence/no name guessing, explicit [] precedence, unknown location, stable identity/isolation/batch identity. Other rematch unit harnesses stub tag initialization because their scope is request/provenance contracts; the browser integration covers the actual seam.
- Production App intentionally not opened; deployment status is checked through Vercel metadata only.


## Structured candidate defaults, candidate cards and top Edit — 2026-09-11

- Supersedes the earlier candidate restaurant-default evidence order: first draft initialization now uses initialCandidateRestaurantTags, honoring existing explicit restaurantTags (including []) before primaryType, mapped secondary types, structured display labels, then existing restaurantTagValues/inferredRestaurantTags fallback. restaurantTagsFromCategory remains the exact canonical mapping (hot_pot_restaurant -> 火鍋); only barbecue_restaurant/korean_barbecue_restaurant -> existing 燒肉 were added. Generic restaurant/food/establishment/point_of_interest and names alone produce no cuisine. No LLM, nearby, area-based cuisine inference or large new classifier.
- Existing /api/places search and exact-details requests now include primaryType/types and pass these plus primaryTypeDisplayName to the candidate. Social search already requested them and now retains them in its response. No new requests/endpoints; API functions remain 12. Maps enrichment no longer prematurely assigns derived empty restaurantTags; classification is committed at the existing first-draft boundary. Trusted structured restaurant evidence may identify an otherwise generic candidate as restaurant, without overriding lodging/shopping.
- areaTags initialization is unchanged: existing AreaTags.suggestions, containment then address/町名 fallback, no semantic aliases. Map.has(identity) remains the only first-init guard; render, selection, Save/Cancel and batch add never reclassify committed drafts, including manually cleared arrays.
- importPreviewMarkup reads candidateDraftStore.get(identity) for the compact wrapping chips (placeTagsList); candidate detail still uses committed draft/placeTagsDetail. Both show area before cuisine and omit empty rows. Formal Places list still reads persisted arrays only.
- Formal detail now has a compact 編輯 button beside the title, reusing data-edit-place -> openPlaceEditSheet. Removed duplicate bottom Edit; vote/maps/delete controls retained. Its existing undo button is placed in the same header action group to avoid blocking the new entry. Save/Cancel return contract, Google identity and filters unchanged.
- Integration script scripts/verify-candidate-tags.cjs: actual initial analysis click -> analyzePlaceImportSheet -> mocked /api/places response -> enrichPlaceImportsFromApi -> candidateDraft -> renderImportPreview/importPreviewMarkup -> candidate card DOM; hot_pot -> 火鍋 + 銀座. Actual rematch click -> rematchImportCandidateGroup -> draft/list before detail open also passes. Candidate edit click -> openImportCandidateEditor -> openPlaceEditSheet/bindPlaceEditor -> actual submit listener -> submitCandidateDraftEditor -> reopenImportCandidateSheet -> updated detail AND list; Cancel, manual clear, reopen, selection and batch persistence exercised. Actual top data-edit-place click -> existing editor -> Save/Cancel -> same detail; one Edit entry, clickable at 393x852 without scroll, no undo overlap, identity preserved. Real handlers/rendering are not stubbed; HTTP/persistence I/O uses local fixtures. Production App not opened.
- Helper/API tests cover primary-over-secondary specificity, display labels, generic/name-only rejection, explicit/manual arrays, existing area fallback, Google field masks and response preservation on search/exact details without extra requests, and social response types. Browser tests prove production boundaries rather than manually chaining helpers.
- Validation in Windows CRLF checkout: targeted 256/257; full regression once 447/448. Sole failure in both is the already confirmed area-tags.test.mjs LF-only source-slicing issue. No new failures. That test, .gitattributes and core.autocrlf untouched; no line-ending normalization. App/script/all 12 API syntax and git diff --check pass. Asset version 20260911.5.
- Canonical source travel-app/prototype/candidate-structured-tags based on fetched origin/main ae2054c; protected dirty worktrees retained. Google type reference: https://developers.google.com/maps/documentation/places/web-service/place-types .

# Trip Planning Final Polish (Phase 1B.2.1) — release round, 2026-09-16

- Worktree `trip-planning-final-polish` (sibling of `trip-deploy`/`trip-planning-ux-refinement`), branch `fix/trip-planning-final-polish`, baseline `origin/main` 512ce8ecb2e2b700cc88aff423f060fed4139fd5 (Phase 1B.2, fetched/verified exact match, reconfirmed unchanged before push). Small, low-scope production-smoke-feedback round: only the two items below, nothing else touched.
- Removed the redundant manual direct-add ➕ (`data-pool-add`, `.place-pool-selected-add`) from `placePoolSelectedCompactCardMarkup` — the one row template shared by the desktop Selected column and the mobile Selected drawer. A selected row is now exactly two buttons: the unselect checkmark (`data-pool-place`) and the name+date-summary row (`data-pool-constraint`, still opens 指定日期 unchanged). The underlying `openPlacePoolAddSheet`/`addPlaceToItineraryDay`/`insertPlacesIntoItineraryDay`/`completePlacePoolAdd` chain and its `data-pool-add` click-delegation branch were deliberately left in place (per this round's own instruction not to dead-code-clean helpers just because a caller went away) — they simply have no UI entry point inside 行程規劃 now. Removed the now-unused `.place-pool-selected-add` CSS rule.
- Added `placePoolScrollSession` (`{ tripId, hasOpened, savedScrollTop }`), trip-scoped in-memory only (same lazy-reset-on-tripId-change pattern as `placePoolSelection`/`placePoolDrawer` — so a trip switch, including switching back to a trip visited earlier in the same session, is always treated as a fresh session and reload trivially resets it too). `setPlacePoolOpen` now: on close, captures `[data-place-pool-list]`'s current `scrollTop` into `savedScrollTop`; on open, sets it to `0` if `!session.hasOpened` (first open this trip session) or `session.savedScrollTop` otherwise, then marks `hasOpened = true`. This is a separate mechanism from the existing `capturePlacePoolAnchor`/`restorePlacePoolAnchor` (which only ever preserves position across a re-render while the workspace stays open, and is completely untouched by this round) — normal selection/filter/constraint-edit renders never touch `placePoolScrollSession` at all, and neither does `setPlacePoolDrawerOpen` (mobile drawer), confirmed both by a source-level regression test and live in the browser (drawer open/close left the main list's scrollTop bit-for-bit unchanged).
- **Reliability fix caught during this round's own browser smoke** (not by the vm test harness, which never fires `window.requestAnimationFrame` at all): the first implementation wrapped the scroll-reset in `requestAnimationFrame`, which a backgrounded/hidden browser tab can throttle indefinitely — reproduced live (the callback never ran while the Browser pane tab was unfocused). Moved the scroll-reset to run synchronously immediately after `render()` returns, the same completion point `capturePlacePoolAnchor`/`restorePlacePoolAnchor` already rely on; the separate `requestAnimationFrame` for focus-management (`.place-pool-close`/`[data-toggle-place-pool]`) was left as-is, unaffected.
- Validation: full regression 772/772 (41 files, up from 767/767; `itinerary-place-pool.test.mjs` net +5 tests — 2 rewritten to drop the ➕ assertions, 5 new covering the removal and the scroll-session tripId-reset/wiring). Canonical-area PRE/POST fingerprints unchanged (zero diff outside `app.js`/`styles.css`/this test file/memory). API functions 12; `node --check`/`git diff --check` pass.
- Local browser smoke (same scratch static+API-stub server pattern, re-pointed at this worktree, Playwright still not installed): confirmed live at 1280×860 and 375×812 — fresh load's first open starts at `scrollTop: 0`; scrolling to a clamped 557px, closing, and reopening restores exactly `557`; switching `state.tripId` away and back to the same trip resets to `0` on the next open (not the previously-saved position); the Selected row (both desktop column and mobile drawer, DOM-inspected directly) has exactly 2 children, no ➕, no `data-pool-add`; the constraint summary still opens the 指定日期 dialog; mobile drawer open/close left the main list's scrollTop completely unchanged (800 throughout); zero console errors; zero `PUT /api/trip` across the entire session (all `/api/trip` traffic was periodic read-only polling). Real iPhone hardware not exercised.

# Trip Planning Workspace UX Refinement (Phase 1B.2) — release round, 2026-09-16

- Worktree `trip-planning-ux-refinement` (sibling of `trip-deploy`, not inside it), branch `feat/trip-planning-ux-refinement`, baseline `origin/main` a206694be555a92781536ebce849d6fc3ebbea2c (fetched and verified, exact match, and reconfirmed unchanged right before this commit). `trip-deploy`'s own dirty WIP and the untouched `fullscreen-trip-planning-workspace` worktree were left completely alone per this round's explicit instructions.
- Product decision, reversing Phase 1B.1's "Selected splits out of the pool" model: the main list is now **every planning-eligible Place, always** — selecting one only changes its own visual state (checkmark, `aria-pressed`, a revealed `指定日期` control) and never removes, reorders, or hides it. `placePoolViewModel()` now returns `mainEntries` (`= pool.entries`, the filtered set, unchanged by selection) alongside the pre-existing `selectedEntries` (still built from the *unfiltered* pool so the desktop column/mobile drawer's Selected Summary is never hidden by the current filter). The old `unselectedEntries` field is gone; `placePoolCardMarkup` takes an explicit `selected` boolean and only renders the `指定日期` control when true — an unselected card never shows a date control at all, closing off the old "two ways to select" bug where the day-wheel's real days silently auto-selected a Place. Empty-state copy now distinguishes 目前篩選條件下沒有地點 (filtered to zero) from 目前沒有可規劃的地點 (nothing eligible at all); it is never shown just because everything is selected.
- Planning constraint model replaced outright: `placePoolPlanningConstraints()` is now `Map<placeDetailKey, dateOptions[]>` (was `Map<key, {dayKey, time}>`), each entry `{ dayKey, mode: "none"|"preferred"|"exact", preferredPeriods: string[], exactTime: "HH:MM"|null }`. `dateOptions = []` means no restriction at all (not a single `UNSCHEDULED` sentinel). `normalizePoolDateOption()` enforces mutual exclusivity (exact wins over a malformed preferred+exact draft; selecting all six `POOL_PREFERRED_PERIOD_KEYS` normalizes to `"none"`) and `applyPlacePoolConstraint(key, dateOptions)` re-sorts into the trip's own day order (`dateMeta`) regardless of input order, and — the other half of the "no second selection path" fix — now fails closed unless the key is **already selected** (`placePoolSelectedKeys().has(key)`), so the date dialog can never itself select a Place. `POOL_PREFERRED_PERIOD_RANGES` defines the six deterministic 00:00–23:59 boundaries (凌晨/上午/中午/下午/晚上/深夜) for a future Planner; nothing schedules against them yet. `placePoolConstraintSummaryText(key, "card"|"summary")` is the one shared formatter (single date → `9/22` / `9/22・偏好上午、下午` / `9/22・18:30`; contiguous all-`none` multi-date → `9/22–9/24`; anything else multi-date → `可選 N 天`; empty → `指定日期` in card context, `未指定日期` in summary context).
- The constraint sheet was rebuilt from a bottom-sheet day/time wheel into `.place-pool-constraint-dialog`, a **centered** modal (`.place-pool-constraint-backdrop { align-items: center }`, never bottom-aligned) titled 指定日期 with the place name, a scrollable list of every trip day as its own checkbox (no `未安排` option — an unchecked day simply isn't in `dateOptions`), a helper line ("未勾選日期時，將交由 AI 自由安排"), and a footer with only 取消/確定. Checking a day reveals an inline 不指定時間/偏好：.../指定 HH:MM summary button; clicking it expands (at most one day at a time) a mode selector (不指定時間/偏好時段/指定時間) plus either the six period chips or the hour/minute exact-time wheel (reusing the itinerary time wheel's exact visual contract under distinct `data-pool-wheel-*` attributes). The old manual "直接加入行程" button is gone from this dialog entirely; that path now lives as its own `＋` button (`data-pool-add`, unchanged `openPlacePoolAddSheet` chain) on the desktop Selected column / mobile drawer row, independent of the date dialog. Draft semantics unchanged in spirit from 1B.1: `pendingPoolConstraint` is copied from the committed value on open, Cancel (×, footer 取消, backdrop click via the existing `data-dismiss-sheet` wiring, or a new Escape handler) discards it via `closeSheet()`, and only 確定 calls `applyPlacePoolConstraint`.
- **Real bug caught by browser smoke, not by the vm test harness** (the harness's `document.querySelector` stub only resolves `#app`/`#sheet-root`/`#toast-root`, so it silently no-ops exactly this class of query and can't catch it): `updatePoolConstraintWheelColumn`'s exact-time commit used to write into `document.querySelector("[data-pool-constraint-time-summary-text]")` — but every *checked* date row carries that same attribute, not just the expanded one, so scrolling the exact-time wheel for a second checked date overwrote the **first** checked date's displayed summary text with the second date's time (the underlying `pendingPoolConstraint.dates` model was always correct; only the live DOM text was wrong). Fixed by scoping the query to the specific expanded day: `` `[data-pool-constraint-expand="${pendingPoolConstraint.expandedDayKey}"] [data-pool-constraint-time-summary-text]` ``. Reproduced and confirmed fixed live (scroll the hour wheel for a second checked date, scroll back up, first date's own 偏好 summary was intact) plus a structural regression test asserting the scoped selector string is present in source.
- Mobile: the candidates column now permanently reserves a 58px right-edge safe area (`padding-right: 58px` under `@media (max-width: 899.98px)`) so a card's `指定日期` control can never render under the fixed 46px-wide Selected-drawer handle — measured via `getBoundingClientRect()` overlap checks at both 375×812 and 320×700 in a real browser (zero overlap, 12px/10px clear gaps, zero horizontal document overflow at either width). The mobile drawer gained a real backdrop (`[data-pool-drawer-backdrop]`, `.place-pool-workspace.is-drawer-open` fades it in, desktop hides it entirely) that closes the drawer on click, and the handle now supports a horizontal drag in addition to the existing click toggle: `poolDrawerDrag` mirrors the pre-existing `swipeDrag` shape (6px axis-lock threshold, `setPointerCapture`, a `suppressPoolDrawerClick` flag so the drag's own trailing click never re-toggles), computing a continuous open `fraction` applied in real time to the drawer's `transform`, the handle's `right` offset, and the backdrop's `opacity`, and snapping via `setPlacePoolDrawerOpen(fraction >= 0.5)` on release. Verified live with real mouse-drag input (`computer` tool's `left_click_drag`, which — unlike hand-dispatched synthetic `PointerEvent`s — registers a genuine active pointer so `setPointerCapture` doesn't throw): 70% open-drag opens it, a subsequent <50% closing drag stays open, a >50% closing drag closes it, and no extra click-triggered re-toggle fired after any drag release.
- Exact-time alignment bug fix (`.place-pool-constraint-time-wheel`): the shared `.time-wheel-column`/`.time-wheel-selection` rule was written for the default 190px-tall wheel (`padding-block: 73px`, selection `top: 73px`), but the constraint version overrides the wheel to 150px tall without recentering either value, so the highlighted selection box sat visibly below the true center. Recentered both to `(150 - 44) / 2 = 53px`. Verified in a real browser via `getBoundingClientRect()`: the selection box's center is within 0.25px of the active option's center, and within 0.01px of the picker's own geometric center.
- Place Editor sheet (`.place-editor-sheet`, opened from Place Detail's 編輯 button) footer fix: it used to be a single `overflow-y: auto` block with a `position: sticky` footer, which only visibly "sticks" once the content is tall enough to need scrolling — for the common short-content case (most lodging/attraction edits) the footer just sat right after the last field, not glued to the panel's true bottom, which is the bug that was reported. Restructured into an explicit flex column: header/fields/photo editor now live inside a new `.place-editor-scroll` (`flex: 1 1 auto; min-height: 0; overflow-y: auto`), with the `.modal-actions` footer as a sibling normal-flow flex item (`flex: 0 0 auto`) after it — pinned to the true bottom at any content length, not just once it overflows. Verified via `getBoundingClientRect()`: footer bottom edge is within 0.001px of the sheet's own bottom edge both before and after scrolling the content region to its end (footer position never moves). `tests/place-editor-navigation.test.mjs`'s existing footer-contract test was rewritten to assert the new flex-column shape instead of `position: sticky`.
- Scroll-anchor preservation (Phase 1B.1's mechanism, untouched code) was re-verified under the new "selected stays in the main list" model, where selecting a Place now measurably grows that specific card's height (the revealed `指定日期` button narrows the space available to the name/category text). Naively comparing raw `scrollTop` before/after looks like drift (correctly so — total content height above the fold really did grow) — the actual hard gate is whether a stable, untouched landmark row keeps the *same screen position*: verified via `getBoundingClientRect().top` on a landmark row before and after 5 selects + 2 unselects in a 35-Place synthetic list, delta exactly 0px in both cases.
- Validation: full regression 767/767 (41 files, up from the 753/753 baseline — `tests/itinerary-place-pool.test.mjs` net +12 tests after replacing every 1B.1-era selection/constraint test with the new dateOptions/main-list-visibility contract; one new `place-editor-navigation.test.mjs` assertion rewritten, not added). Canonical-area Phase A/B/C safety unchanged (nothing in `lib/`/`data/`/`api/`/`vercel.json` touched — confirmed by `git diff --stat`); PRE `1310fa0c…825f` / POST `dd2d0b93…6893` unchanged. API functions 12; `node --check`/`git diff --check` pass.
- Local browser smoke: Playwright is still not installed in this sandbox. As the established substitute, a new scratch static+API-stub Node server (serving this worktree's real files, a synthetic 9→35-Place trip, zero real network/Redis — the real `api/*.mjs` handlers need live Upstash credentials and can't run standalone) plus the Browser pane tool drove the actual app at 1280×860, 375×812 and 320×700. Covered: main-list-keeps-selected-visible with live position/`aria-pressed` checks, the empty-state copy split, the centered 指定日期 dialog end to end (multi-date, 偏好時段 multi-select with live chip highlighting, mode switching, exact-time wheel), the cross-row summary-text bug above, mobile handle/date-control overlap geometry, drawer backdrop + real mouse-drag open/close/reverse-drag/no-double-toggle, the Place Detail footer geometry, and the scroll-anchor landmark check. Real iPhone hardware not exercised. Production App was not opened; 0 AI/Google Places calls; 0 production data touched.

# Fullscreen Trip Planning Workspace + Planning Constraints (AI Planner Phase 1B.1) — release round, 2026-09-16

- This round closed out the review gate below: the migration-toast presentation fix and the real-browser 30–40-Place scroll-preservation verification it had been waiting on. See `memory/project_state.md`'s top entry for the full release summary (migration toast source/fix, browser verification method and results, regression accounting, commit/push).
- Verification method note for next time: drive the interaction with real DOM `.click()`/`scrollTop` calls via the Browser pane's `javascript_tool` rather than coordinate-based `computer` clicks when doing more than a handful of sequential selections on a long list — it's far more reliable at scale — but always re-`querySelector` the container fresh after any action that can trigger `render()` (which replaces `#app`'s entire `innerHTML`); a cached container/element reference from before that render becomes a detached node and will silently read back stale/zeroed values, which looks exactly like a false "scroll jumped to top" regression. Caught and corrected once during this round before it was reported as a bug.

# Fullscreen Trip Planning Workspace + Planning Constraints (AI Planner Phase 1B.1) — review gate, 2026-09-16

- Worktree `fullscreen-trip-planning-workspace` (sibling of `trip-deploy`, not inside it), branch `feat/fullscreen-trip-planning-workspace`, baseline `origin/main` b7df7338cced3a9d64c921aeda9bb1143c2c19d3 (fetched and verified, exact match). Uncommitted; awaiting review. No commit, push or deploy. `trip-deploy`'s own dirty WIP (`api/social-place-import.mjs`, etc.) was left completely untouched per the project's established convention.
- Product decision: 地點池 becomes 行程規劃 — a full-viewport workspace (not a bottom sheet / desktop side-dock) that is the shared container for selection today and AI Planner/Preview/Discovery later. Desktop: static two-column layout, left Selected (compact rows, independently scrollable, never auto-collapses) / right candidates + filters. Mobile: the main workspace shows only filters + candidates; Selected becomes a right-side drawer behind an always-visible (including at 0 selected) fixed right-center handle, 46×100px touch target, Chinese label/count stacked as separate lines (never rotated), chevron `‹`/`›` and position tracking the drawer's open/closed edge via a pure CSS attribute selector (`.place-pool-workspace.is-drawer-open`).
- New client-memory-only planning constraint per selected Place: `placePoolPlanning` is a `{ tripId, constraints: Map<placeDetailKey, {dayKey, time}> }`, the same trip-scoped lazy-reset pattern as `placePoolSelection`. `placePoolConstraintKind()` derives UNSCHEDULED (`dayKey: null`) / FIXED_DAY (`dayKey` set, `time: null`) / FIXED_TIME (`both set`) purely from that shape; `placePoolConstraintDisplayText(key)` is the one formatter every candidate card, the desktop Selected column and the mobile drawer all call (未安排 / 9/22 / 9/22・18:30). Unselecting a Place deletes its constraint outright (`clearPlacePoolConstraint`, also run by the same stale-key pruning `placePoolSelectedEntries()` already did for selection); choosing a real day on an unselected Place auto-selects it (`applyPlacePoolConstraint`); choosing 未安排 never unselects. A mobile Selected drawer's own checkmark button reuses the exact `data-pool-place`/`togglePlacePoolSelection` contract candidate cards use, so a Place can be unselected without returning to its candidate card.
- Constraint editor: `openPlacePoolConstraintSheet(key)` opens a sheet reusing the itinerary time-wheel's exact visual/interaction contract (click-to-scroll, delegated `scroll` listener commit, 44px rows) under distinct `data-pool-wheel-*` attributes and a separate `pendingPoolConstraint` — a day wheel scoped to `dateMeta` (未安排 + only the trip's own dates, never out of range) plus an optional 15-minute-stop time wheel (hour 00–23, minute 00/15/30/45) that only appears once a real day is picked; data is always stored as exact `HH:MM`. Confirm commits via `applyPlacePoolConstraint` and closes with zero network/persist; Cancel discards the pending edit entirely. The previous card-level ⋯/＋日期 manual-add button was replaced by a "直接加入行程" secondary action *inside* this sheet (`data-pool-add`, unchanged `openPlacePoolAddSheet`/`addPlaceToItineraryDay`/`insertPlacesIntoItineraryDay`/`persist()` chain) — editing a constraint and directly adding to a day are visibly distinct actions, never conflated.
- Scroll-anchor preservation (the round's hard-gated bug fix): `placePoolScrollAnchorFromRects(rects, scrollTop)`/`placePoolScrollTopFromAnchor(rects, anchor)` are pure functions over `{key, top}` arrays — find the topmost visible entry plus its immediate neighbor keys (for a deterministic fallback when that exact entry was the one just selected/removed) and reproduce the same visual offset afterwards. `capturePlacePoolAnchor`/`restorePlacePoolAnchor` wire that math to the live DOM (`getBoundingClientRect`) around every `render()`, replacing the old raw-`scrollTop` restore for both `[data-place-pool-list]` and `[data-place-pool-selected-list]`. The pure math has full unit coverage (exact/neighbor-fallback/empty/negative-clamp cases); the DOM wiring itself only runs in a real browser (the sandbox's `document.querySelector` stub only resolves `#app`/`#sheet-root`/`#toast-root`) and was confirmed live via the Browser pane tool against a scratch static+API-stub server (see below) — selecting/unselecting/setting a constraint never moved the candidate list's visual scroll position.
- Removed entirely: the docked-beside-the-phone desktop side panel (`body.place-pool-docked`/`.stage{padding-right:408px}`), the dimmed backdrop, and the >6-selected auto-collapse toggle (`state.placePool.selectedExpanded`, `data-pool-selected-toggle`) — the Selected column/drawer now always renders in full and scrolls independently at any count. `state.placePool` keeps only `{open, kind, section, favoriteOnly}`; the mobile drawer's own open/closed state lives in a new trip-scoped `placePoolDrawer`/`placePoolDrawerIsOpen()`/`setPlacePoolDrawerOpen()` (same lazy-reset-on-tripId-change pattern as selection/constraints), not inside `state.placePool`, specifically so it resets to closed on a trip switch without needing `clearTripView()`.
- Validation: full regression once, 752/752 (41 files, 0 removed/skipped) — up from the 06a1f6ed baseline's 720/720 because `itinerary-place-pool.test.mjs` grew from 44 to 59 tests covering the new fullscreen architecture, mobile drawer/handle, planning-constraint state machine (UNSCHEDULED/FIXED_DAY/FIXED_TIME, auto-select-on-date, clear-on-unselect, fail-closed on stale keys, trip-date-range-only options) and the scroll-anchor pure math; `ui-logic.test.mjs` 67/67 (one assertion's function-boundary marker updated for the renamed `placePoolSelectedCompactCardMarkup`). Canonical-area Phase A/B/C safety 18/18, PRE `1310fa0c…825f` / POST `dd2d0b93…6893` unchanged (nothing in `lib/`/`data/`/`api/`/`vercel.json` was touched — confirmed by `git diff --stat`). API functions 12; `node --check`/`git diff --check` pass.
- Local browser smoke: Playwright is still not installed in this sandbox, so `tests/itinerary-place-pool-smoke.cjs` was rewritten to the new fullscreen/two-column/drawer/constraint-sheet contract but not executed here. As the established substitute, a scratch static+API-stub Node server (serving this worktree's real files, synthetic trip, zero real network) plus the Browser pane tool drove the actual app at 1280×860 and 375×812: fullscreen open/close (workspace fills the real viewport, breaking out of the decorative phone-frame chrome, confirmed via `getBoundingClientRect` against the viewport size), desktop two-column layout with the Selected column strictly left of candidates, select/unselect from both the candidate card and the Selected column's own checkmark, the constraint sheet's day-wheel → time-toggle → time-wheel → confirm flow landing exactly on `9/22・18:30` (verified against `pendingPoolConstraint`/`placePoolConstraintFor` ground truth, not just pixels), zero `PUT /api/trip` calls across every selection/constraint interaction (checked via `read_network_requests`), the mobile right-center handle present at 0 selected with a measured 46×100px hit target, drawer open/close sliding from the right with the handle tracking its edge, drawer rows opening the same constraint sheet, and zero horizontal document overflow at 375px. One real bug was caught and fixed by this pass: `scrollTo({top:0})` on an already-at-0 wheel column never fires a native `scroll` event, so the initial 未安排/hour/minute option never got its `.active` class — fixed with an explicit `syncPoolConstraintWheelsFromScroll()` call after opening the sheet and after revealing the time wheel. Real iPhone hardware not exercised.

# Itinerary Place Pool (AI Planner Phase 1A) — review gate, 2026-09-15

- Worktree `travel-app/prototype/itinerary-place-pool`, branch `feat/itinerary-place-pool`, baseline `origin/main` 59b854bf461e8b863b3698aa67b79e5c67681cfb (fetched and verified). Uncommitted; awaiting review. No commit, push or deploy.
- Product decision: Places → 尚未安排地點池 → manual add now, AI candidate selection later. AI will pick a scheduled subset from candidates and may leave the rest unscheduled; only an explicit hard requirement (e.g. 最想去一定要排) forces scheduling.
- 尚未安排 = the Place's name appears in no itinerary day (existing Place ↔ name contract, flights ignored). Names shared by several Places fail closed: excluded from the pool and only counted.
- One element serves both layouts: a bottom sheet by default; docked beside the phone frame when `(min-width: 900px) and (hover: hover) and (pointer: fine)`. Entry is its own row above the date strip so the itinerary header is unchanged.
- Filters are client view state `state.placePool` (never persisted): 主要地區 (Planning Geography sectionKey via the existing `placesFilterModel`), 地點類型 (lodging stays available), 最想去 = vote count ≥ 1.
- Adds: desktop native drag only when docked (drop on a date button or the day's timeline/empty state); click and touch tap open the existing 加入某一天 sheet in `place-pool` mode. All three call `addPlaceToItineraryDay` → `insertPlacesIntoItineraryDay`, which is the 加入地點 sheet's contract extracted unchanged (90-minute suggestions, before the return flight) and now shared by that sheet. Items stay `{ id, name, time }`; persistence is the existing `persist()` debounced Trip PUT. Place Detail 加入某一天 keeps its own 11:00/15:00 behavior.
- AI foundation: `aiCandidatePlaceKeys()` is trip-scoped in-memory `placeDetailKey` set; `placePoolCandidateEntries()` filters the same pool. No UI, storage or network.
- Test contract change: `tests/ui-logic.test.mjs` "custom solid drag" no longer bans `draggable="true"` globally; it now requires no draggable in the itinerary timeline and exactly one draggable, the docked-gated pool card.
- Validation: baseline 693/693 (40 files). Final full regression 711/711 (41 files, +18 new, 0 removed, 0 skipped). Targeted 18/18; itinerary+identity 111/111; Planning Geography 124/124; Place Interaction 21/21; Phase A 18/18 (derived PRE/POST); Phase B/C 61/61. PRE 1310fa0c…825f and POST dd2d0b93…6893 unchanged; no lib/api/data/vercel diff; API functions 12; syntax and `git diff --check` pass.
- Local browser smoke `tests/itinerary-place-pool-smoke.cjs` (headless Edge, intercepted loopback, synthetic trip): desktop 1280x860 and mobile 390x844 touch pass. Place Pool phase network = Trip PUT only. Startup `POST /api/places` is the existing restaurant tag backfill, identical on the unmodified baseline. Real iPhone hardware not exercised.

## Selection-first Place Pool (AI Planner Phase 1B.0) — review gate, 2026-09-16

- Reason for this round: Phase 1A shipped a manual add-to-day flow, but production UX review rejected forcing the user to pick a day for every Place one at a time. New primary flow: tap Places to build a "確定想去" set, then a sticky CTA hands that set to the (not-yet-built) AI Planner, which owns date/area/order.
- Worktree `travel-app/prototype/selection-first-place-pool`, branch `feat/selection-first-place-pool`, baseline `origin/main` 06a1f6ede967e9a8730a494193d0ff177f49bd84 (fetched and verified, exact match). Uncommitted; awaiting review. No commit, push or deploy.
- `aiCandidateSelection`/`aiCandidatePlaceKeys`/`placePoolCandidateEntries` are renamed to `placePoolSelection`/`placePoolSelectedKeys`/`placePoolSelectedEntries` and their semantics flipped: selected is now HARD (the AI Planner must schedule it exactly once), not "candidate-only". Still trip-scoped in-memory `placeDetailKey` keys on `state.tripId`, never persisted/stored/sent; `placePoolSelectedEntries()` also lazily prunes any key whose Place left the pool (scheduled elsewhere, deleted, or turned same-name ambiguous), so the displayed count is always "currently valid entries", never the raw `Set.size`.
- Card click semantics inverted: tapping/clicking the card body now toggles selection (`aria-pressed`) instead of opening the add-a-day sheet. The old manual-add path is now a secondary action: a sibling `[data-pool-add]` button (＋日期 docked / ⋯ touch) still opens the unchanged `openPlacePoolAddSheet` → `addPlaceToItineraryDay` → `insertPlacesIntoItineraryDay` → `persist()` chain. Desktop drag moved to a dedicated sibling handle (`[data-pool-drag]`, ⠿, docked-only, `aria-label`); the selectable card surface is never `draggable`, so click-to-select and native drag can never race on the same element.
- Rendering: `placePoolViewModel()` splits `getFilteredPlacePool()`'s entries into an always-visible Selected section (built from the unfiltered pool, so a selected Place stays visible with a "不在目前篩選" note when it stops matching 主要地區/地點類型/最想去) and a filtered 其他地點 section. Both preserve `getUnscheduledPlaces()`'s stable order regardless of selection/click order; unselecting returns a card to that same position. Past 6 selected entries the section collapses behind one `已選 N 個 ▾` toggle (`state.placePool.selectedExpanded`) so a long selection cannot bury the rest of the pool, especially on mobile.
- Sticky CTA (`[data-pool-cta]`) reads "AI 幫我規劃行程" at 0 selected or "用已選 N 個地點規劃" above 0, with an "AI 規劃準備中" hint. It ships `disabled`/`aria-disabled` and has no click handler at all: 0 network, 0 persist, 0 itinerary mutation in this round. No OpenAI/AI Planner call was added anywhere.
- Validation: full regression 720/720 (41 files, 0 removed/skipped). Targeted `itinerary-place-pool.test.mjs` 27/27 (new selection tests) + `ui-logic.test.mjs` 67/67 (draggable contract now: handle-only, sibling of the selectable button, never nested). Itinerary+identity 40/40; Planning Geography 118/118; Place Interaction 21/21; Phase A/B/C safety 79/79 (PRE `1310fa0c…825f` / POST `dd2d0b93…6893` unchanged, confirmed in isolation too). No `api/lib/data/vercel.json` diff; API functions 12; `node --check`/`git diff --check` pass.
- Local browser smoke: Playwright is not installed in this sandbox, so `tests/itinerary-place-pool-smoke.cjs` was updated (handle-based drag, `[data-pool-add]` secondary action, multi-select + filter + CTA assertions) but not executed here — it needs a real Playwright install to run. As a substitute, a scratch static+API-stub Node server plus the Browser pane tool drove the actual synthetic trip at 1280×860 and 375×812 (mobile touch preset): click/tap-select, aria-pressed, Selected-section stacking, filter-independent visibility with the "不在目前篩選" note, unselect-returns-to-position, handle-only drag-to-day, secondary-action manual add, and CTA copy at 0/1/2/3 selected all confirmed live, with zero non-PUT network calls during selection and zero horizontal overflow at mobile width. Real iPhone hardware not exercised.
